# @hanger-source/ab-runtime-darwin-arm64

Native `ab-runtime` binary for macOS arm64. This package is selected automatically by `@hanger-source/ab`; it has no public lifecycle CLI and performs no postinstall download.
